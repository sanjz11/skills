# Generic layered pattern

Use `technology_context.profile.layerMapping` labels as JSON keys under `layers`.

Render IR elements into the nearest layer. Use generic pseudocode only.

See `Design-json-shape.json` for output skeleton.
