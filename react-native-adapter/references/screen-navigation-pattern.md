# Screen & navigation pattern (React Native)

Map IR presentation layer → screens and navigation stacks.

| IR capability | Screen |
|---------------|--------|
| User flow step | Screen component |
| apiOperations (read) | Data hook + API client method |

Use TypeScript/JSX pseudo-code in `.md` companion file.
